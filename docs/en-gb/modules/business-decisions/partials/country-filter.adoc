=== Country

Filters the selected key figure and dimension additionally by one or several countries {filtered-data}. Select a comparison operator on the left and then the desired countries (material:check_box[role=skyBlue]).