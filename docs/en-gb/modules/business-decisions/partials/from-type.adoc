=== From type

Filters the selected key figure and dimension additionally by one or several types. Select a comparison operator on the left and then the desired types (material:check_box[role=skyBlue]).
