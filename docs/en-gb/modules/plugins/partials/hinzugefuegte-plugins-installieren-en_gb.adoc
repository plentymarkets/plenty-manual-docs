[NOTE]
.Installing added plugins
====
After adding a plugin to the system, you have to install it in a plugin set before you can use it.

For more information on how to install plugins, see xref:plugins:installing-added-plugins.adoc#[Installing added plugins in plugin sets].
====